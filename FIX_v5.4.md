# الإصلاحات — النسخة 5.4 (تصلح النقلات الغلط + مشكلة الـ 5GB)

## ملخص المشاكل اللي اتحلت

### 1) نقلات غلط لما تلعب بالأسود (أخطر باج) 🔴
**السبب:** البرنامج كان بيبني الـ FEN صح من موقع chess.com، وبعدين **بيقوم بقلب اللوحة 180° مرة تانية** لأن اللوحة معكوسة على الشاشة. النتيجة: المحرك بيحلل وضع مرآة معكوسة → يقترح نقلات غلط تمامًا، بالذات لما تكون أسود.

**الإصلاح:** القارئ (reader.js) بقى يبعت اللوحة **بإحداثيات مطلقة دايمًا** (الأبيض على الصفين 1-2) مهما كان اتجاه العرض، والقلب المزدوج في Swift اتشال. كمان:
- **lichess كان مش شغال خالص** من الأول (حساب الـ translate كان غلط بالقسمة على 12.5) — اتصلح وبيدعم النسب والبكسل.
- كشف النقلة الأخيرة بقى يتحسب بعد تصحيح الاتجاه → النقلة المسجّلة صحيحة حتى على لوحة معكوسة.

### 2) طريقة الـ FEN — خلل مؤكد واتصلح 🟡
- مربع الـ **en-passant** كان بيفضل "لاصق" من نقلة قديمة لو البرنامج فاتته فريمات → دلوقتي بيتصفّر.
- **عدّاد النقلات النصفي (halfmove)** كان بيكبر غلط بعد فريمات مفقودة → بيتصفّر.
- **FenValidator** بقى صارم: مربعات ملك متلاصقة، بيادق على صف غير قانوني، عدد بيادق/قطع غلط، حقل الدور/التبييت — أي قراءة تالفة بتظهر كتحذير "Board read unstable" بدل ما البرنامج ينصح بنقلة على وضع غلط بصمت.

### 3) البرنامج بيجمع داتا أكتر من 5GB 🔵
**السبب:** مش الكاش الداخلي ولا السجل (دي سليمة ومقفولة) — المشكلة في **بيانات WebKit بتاعة الموقع** (كاش chess.com + Service Workers) في `Library/WebKit` — كانت **مش ظاهرة في تقرير المساحة أصلًا** ولا زرار المسح القديم بيمسها.

**الإصلاح:**
- تقرير المساحة دلوقتي بيعرض سطر "Web data (WebKit)" بالحجم الحقيقي.
- **تنضيف تلقائي عند كل تشغيل** للكاش (من غير مسح تسجيل الدخول) → البرنامج مش هيرجع يكبر تاني.
- زرار جديد في الإعدادات: **"مسح بيانات المتصفح (المساحة الكبيرة)"** — بيمسح كل حاجة ويسلّمك الجيجابات/الجيجا المهدرة (هتسجل خروج من الموقع).
- كاش الشبكة بقى في الرام بس (مش بيكتب على الديسك أصلًا).

## إزاي تطبّق الإصلاح على GitHub (دقيقة واحدة)

صلاحية الوكيل هنا مش بتسمح لي أرفع تعديل على ملفات workflow مباشرة، فاعملها بنفسك من المتصفح — سهلة:

1. افتح الملف الجاهز: [`ChessHelper-build-v5.4.yml`](ChessHelper-build-v5.4.yml) → زرار **Copy** (أو اعمل Download).
2. افتح على GitHub: `https://github.com/ahcrazy20-tech/chess3/edit/main/.github/workflows/build.yml`
3. امسح كل المحتوى القديم والصق الجديد → **Commit changes**.
4. هيبدأ البيلد تلقائيًا (Workflow name: Build ChessHelper v5.4) وهتلاقي `ChessHelper-v5.4-Phase6.ipa` في **Actions → آخر تشغيل → Artifacts**.

> أو من الترمينال لو بتحب git:
> ```bash
> git am patches/v5.4-fix.patch && git push
> ```

## الاختبارات (كلها ناجحة ✅)
- `tests/test_reader.js` — 26 فحص بمحاكاة DOM: chess.com عادي/مقلوب، lichess عادي/مقلوب، نقلات e2e4/e7e5، الدور الصح. شغّلها بـ node.
- `tests/test_fen.py` — 19 فحص لبورت مطابق لمنطق FenBuilder/FenValidator: en-passant، التبييت، عدّاد النقلات، كل فحوصات Validator. شغّلها بـ python3.
